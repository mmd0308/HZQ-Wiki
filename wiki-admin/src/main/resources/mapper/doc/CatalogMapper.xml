<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzqing.admin.mapper.doc.CatalogMapper">

    <resultMap id="baseResult" type="catalog">
        <id column="id" property="id"/>
        <result column="doc_id" property="docId" />
        <result column="parent_id" property="parentId" />
        <result column="name" property="name"/>
        <result column="sequence" property="sequence" />
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="tableName">
        hw_doc_catalog
    </sql>

    <select id="selectList" resultMap="baseResult" parameterType="catalog">
        select
            id,
            doc_id,
            parent_id,
            name,
            sequence,
            create_time,
            update_by,
            update_time
        from 
            <include refid="tableName" />
        <where>
            <if test="docId != null">
                doc_id = #{docId}
            </if>

        </where>
    </select>

    <insert id="insert" parameterType="catalog" >
        INSERT INTO
            <include refid="tableName" />
        (
            doc_id,
            parent_id,
            name,
            sequence,
            create_by,
            create_time,
            update_by,
            update_time
        ) VALUE (
            #{docId},
            #{parentId},
            #{name},
            #{sequence},
            #{createBy},
            #{createTime},
            #{updateBy},
            #{updateTime}
        )
    </insert>

    <delete id="deletedById" parameterType="string">
        DELETE FROM
            <include refid="tableName" />
        WHERE id = #{id}
    </delete>

    <update id="update" parameterType="catalog">
        UPDATE
            <include refid="tableName" />
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="spaceId != null">
                space_id = #{spaceId},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
            <if test="visitLevel != null and visitLevel != ''">
                visit_level = #{visitLevel},
            </if>
            <if test="img != null and img != ''">
                img = #{img},
            </if>
            <if test="createBy != null and createBy != ''">
                create_by = #{createBy},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateBy != null and updateBy != ''">
                update_by = #{updateBy},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
